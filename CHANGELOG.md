# 更新日志

本项目版本号采用语义化版本（SemVer），遵循 [Keep a Changelog](https://keepachangelog.com/zh-CN/1.0.0/) 格式。

## [4.0.2] - 2026-08-12

### 🎉 新增

- **音乐悬浮球**：播放音乐时右下角显示悬浮球，单击暂停 / 继续，双击打开控制器
- **音乐控制器**：切歌（上一首 / 下一首）、倍速（0.5x–2x）、暂停、进度条拖拽、音量调节
- **悬浮球个性化设置**：可单独调整悬浮球圆角，或选择跟随全局按钮圆角（默认不跟随）

### 🐛 修复

- **字体下载后无法使用**：修复 `rlv-font://` 协议 CORS 问题，下载的字体真正可渲染；启动时重新注入持久化的按需字体；修复字体下拉异步闭包读到默认值导致"选不中"
- **设置页白屏**（`jvmArgs` / `gameArgs` 数据规范化，含配置文件写入 `{}` 的根因修复）

### 🌐 联机

- **邀请码对齐 Terracotta / HMCL**：`U/XXXX-XXXX-XXXX-XXXX` 格式，可与其他启动器的陶瓦联机互通
- 网络名 `scaffolding-mc-*`、主机名 `scaffolding-mc-server-*`，经 EasyTier 公共节点入网 + peer 发现主机

### 🏗️ 工程

- `shell.openPath` 路径校验改用 `path.relative`（防目录穿越）
- 日志清洗 Error 换行
- 联机本机 IP 增加 UDP 出口探测与回退
- 启动失败状态显式收尾（`launchStore.fail`）
- 下载页加载器按钮区分"安装加载器"
- 构建输出结构调整（`tsconfig rootDir`）
- README / 关于界面澄清 EasyTier 与 Terracotta 的定位

## [4.0.1] - 2026-08-12

### 🐛 修复

- **LittleSkin 外置登录**：登录失败时弹窗显示具体原因（账号或密码错误 / 无法连接认证服务器 / 服务器地址有误 / 无可用游戏角色），不再静默关闭回到启动页；登录成功才关闭对话框
- **最大化窗口缩放**：内容区按窗口宽度等比放大填满屏幕，修复根元素缩放导致的"滚动条滚不到底部"
- **配置数据修复**：`jvmArgs` / `gameArgs` 不再被 `{}` 覆盖写回配置文件

## [4.0.0] - 2026-08-12

### 🎉 新增

- **联机系统**：基于 EasyTier 的 P2P 虚拟局域网 + Terracotta / Scaffolding 协议的房间联机
  - 创建房间（校验局域网游戏端口）与邀请码加入
  - 真实局域网组播发现（`Open to LAN` 端口自动检测）
  - 玩家列表、连接质量（NAT 类型）展示
- **整合包导出**：支持导出 Mod / 资源包 / 光影 / 存档 / 截图 / 选项文件，可**按具体 Mod 勾选导出**
- **Modrinth 模组搜索与下载**：按游戏版本与加载器筛选
- **日志页面**：实时查看 / 清空 / 打开日志文件夹
- **背景音乐播放器**：本地音乐目录播放、音量控制
- **可移植数据目录** + 用户可编辑 `.js` 配置系统（`.RLV/` 便携模式）
- **按需字体下载**：思源黑体 / 枫叶等宽等字体按需拉取
- **下载镜像（BMCLAPI）**：国内用户加速下载
- **版本筛选**：可编辑输入 + 默认显示全部版本
- **关于界面**：开源致谢 + **各项目许可证跳转链接**

### 🐛 修复

- 版本详情「设置」页打开白屏（`jvmArgs`/`gameArgs` 配置数据规范化）
- 联机「创建房间」卡片 hover 只有上半部分发光
- 渲染进程 `process.env` 访问崩溃防护（`config.ts` / `fonts.ts`）

### 🏗️ 工程

- **CI（GitHub Actions）**：每次 push / PR 自动运行 ESLint、Prettier、构建检查；推送 `v*` tag 自动构建并发布 Windows 安装器
- **electron-builder**：发布配置补全（`releaseType: release`、`publisherName`）
- **electron-updater**：错误处理健壮化，更新检查失败不再影响主界面加载
- 全仓库 Prettier 统一格式化
